databaseChangeLog:
  # Create tables
  - includeAll:
      contextFilter: all
      path: db/all

  # Insert data for development and integration tests
  - includeAll:
      contextFilter: dev,test
      path: db/dev
