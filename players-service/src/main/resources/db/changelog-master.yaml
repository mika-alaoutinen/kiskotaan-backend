databaseChangeLog:
  # Create tables
  - includeAll:
      contextFilter: all
      path: db/all

  # Insert data for development
  - includeAll:
      contextFilter: dev
      path: db/dev

  # Insert data for integration tests
  - includeAll:
      contextFilter: test
      path: db/test
