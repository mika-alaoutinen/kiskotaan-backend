# Don't configure DB connection and let Quarkus dev mode handle it
quarkus:
  hibernate-orm:
    validate-in-dev-mode: true

  http:
    port: 8082

mp:
  messaging:
    outgoing:
      courses-out:
        topic: "courses"
      holes-out:
        topic: "holes"
